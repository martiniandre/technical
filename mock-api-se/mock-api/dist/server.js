"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const http_1 = __importDefault(require("http"));
const express_1 = __importDefault(require("express"));
const config_1 = require("./config");
const cors_1 = require("./middleware/cors");
const auth_1 = require("./middleware/auth");
const auth_2 = require("./routes/auth");
const dashboard_1 = require("./routes/dashboard");
const health_1 = require("./routes/health");
const websocket_1 = require("./websocket");
const app = (0, express_1.default)();
app.use(cors_1.openCors);
app.options('*', (_req, res) => res.sendStatus(204));
app.use(express_1.default.json());
app.post('/login', auth_2.login);
app.get('/health', health_1.health);
app.get('/dashboard', auth_1.authenticateToken, dashboard_1.dashboard);
const server = http_1.default.createServer(app);
(0, websocket_1.createWebSocketServer)(server);
server.listen(config_1.config.port, () => {
    const sep = '='.repeat(60);
    console.log(sep);
    console.log('Mock API Server');
    console.log(sep);
    console.log(`  REST API :   http://localhost:${config_1.config.port}`);
    console.log(`  WebSocket:   ws://localhost:${config_1.config.port}`);
    console.log(`  Health   :   http://localhost:${config_1.config.port}/health`);
    console.log(sep);
    console.log('  Login credentials:');
    console.log('    admin    / admin123  (role: administrator)');
    console.log('    engineer / eng456    (role: engineer)');
    console.log('    viewer   / view789   (role: viewer)');
    console.log(sep);
    console.log(`  WS auth  :   ws://localhost:${config_1.config.port}?token=<JWT>`);
    console.log(`  WS update:   every ${config_1.config.wsUpdateIntervalMs / 1000}s`);
    console.log(sep);
});
